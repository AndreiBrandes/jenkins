pipeline {
    agent any
    stages {    
      stages {
          stage('Cloning our Git') {
             steps {
                 git 'https://github.com/AndreiBrandes/jenkins.git'
                }
             }
        }
}
}